on: push
name: 🚀 Publish Website over SFTP
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@main
    - name: 📂 Sync files
      uses: Automattic/FTP-Deploy-Action@v3.0.1
      with:
        ftp-server: sftp://sftp.wp.com:22/htdocs/
        ftp-username: ${{ secrets.APPA_WORDPRESS_SFTP_PASS }}
        ftp-password: ${{ secrets.APPA_WORDPRESS_SFTP_USER }}
